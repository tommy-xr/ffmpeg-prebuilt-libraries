name: Build FFmpeg (MacOS, Intel)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      ffmpeg_version:
        description: "FFmpeg upstream tag to build"
        required: false
        default: "n7.0"
  workflow_call:
    inputs:
      ffmpeg_version:
        required: false
        type: string
        default: "n7.0"

jobs:
  build:
    name: Build FFmpeg for MacOS
    # macos-13 runners are intel based
    runs-on: macos-13

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Set up dependencies on macOS
        run: |
          brew update
          brew install \
            autoconf automake cmake fdk-aac git lame libass libtool \
            libvorbis libvpx opus sdl2 texinfo wget x264 x265 xvid yasm

      - name: Clone ffmpeg
        uses: ./.github/actions/ffmpeg-clone
        with:
          version: ${{ github.event_name == 'workflow_call' && inputs.ffmpeg_version || github.event_name == 'workflow_dispatch' && github.event.inputs.ffmpeg_version || 'n7.0' }}

      - name: Build FFmpeg on MacOS (Intel, x86_64)
        run: |
          cd ffmpeg
          ./configure --enable-shared --enable-static --disable-doc --arch=x86_64 --prefix=out-x86_64
          make -j1
          make install

      - name: Package artifact
        run: |
          tar -czf ffmpeg-macos-x86_64.tar.gz -C ffmpeg/out-x86_64 .

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ffmpeg-macos-x86_64
          path: ffmpeg-macos-x86_64.tar.gz
